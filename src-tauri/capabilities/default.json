{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the automation control window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
